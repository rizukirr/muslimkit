#pragma once

#include <ctype.h>
#include <stdio.h>

/**
 * @brief convert string into lowercase
 *
 * Convert string into lowercase by looping a string
 * and apply tolower()
 */
void to_lowercase(char *str);
